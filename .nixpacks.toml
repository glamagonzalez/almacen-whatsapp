[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mysqli", "php82Extensions.mbstring"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader || echo 'Composer install skipped'"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."
